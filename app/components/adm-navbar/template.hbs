<div class="block lg:hidden">
  {{#basic-dropdown horizontalPosition="left" as |dd|}}
    {{#dd.trigger}}
      {{svg-jar "menu" class="icon"}}
    {{/dd.trigger}}

    {{#dd.content class="adm-navbar-burger-content"}}
      {{component
        linkComponent
        "AdamGribble.com"
        homeRoute
        class="font-thin no-underline p-2 rounded text-orange text-xl tracking-wide"
      }}
      {{yield (component linkComponent class="block mt-4 lg:inline-block lg:mt-0")}}
    {{/dd.content}}
  {{/basic-dropdown}}
</div>
<div class="hidden lg:flex items-center flex-no-shrink mr-6">
  {{component
    linkComponent
    "AdamGribble.com"
    homeRoute
    class="font-thin no-underline p-2 rounded text-orange text-xl tracking-wide"
  }}
</div>
<div class="hidden w-full block flex-grow lg:flex lg:items-center lg:w-auto">
  <div class="text-sm lg:flex-grow">
    {{yield (component linkComponent class="block mt-4 lg:inline-block lg:mt-0")}}
  </div>
</div>